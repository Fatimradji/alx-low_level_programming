i#include <stdio.h>

/**
 * main - start of program
 * @argc: argument count
 * @argv: argument vector storing strings
 * Return: on (0) successful, any other value an error.
 */

int main(int __attribute__((unused)) argc, char __attribute__((unused)) **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}

