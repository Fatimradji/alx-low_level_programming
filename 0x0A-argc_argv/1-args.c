#include <stdio.h>

/**
 * main - start of program
 * @argc: count integers
 * @argv: strings in array
 * Return: successful as (0)
 */

int main(int __attribute__((unused)) argc, char __attribute__((unused)) **argv)
{
	printf("%d\n", (argc - 1));

	return (0);
}
